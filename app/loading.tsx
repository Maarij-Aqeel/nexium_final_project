import Loading from "@/components/Loading";

export default function LoadingPage() {
  <div className="bg-hero-gradient items-center flex">
    <Loading msg1="Getting the page ready..." />;
  </div>;
}
